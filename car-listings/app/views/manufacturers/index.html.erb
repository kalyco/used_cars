<h2> Manufacturers </h2>
<ul>
  <% @manufacturers.each do |manufacturer| %>
    <li><%= link_to "#{manufacturer.name}", manufacturer_path(manufacturer) %>
    <%= manufacturer.country %></li>
  <% end %>
</ul>

<%= link_to "add manufacturer", new_manufacturer_path %>
